# Hello 算法 - 本地开发指南

## 环境依赖

| 工具 | 版本要求 | 用途 |
| ------ | ---------- | ------ |
| Python | 3.10+ | 运行 MkDocs 文档服务 |
| Node.js | 24.x | 执行 JavaScript 示例代码 |
| Deno | 2.x | 运行 JavaScript 测试脚本 |

---

## 一、文档服务（MkDocs）

### 1.1 首次初始化

首次使用需复制 `docs/` 和 `overrides/` 到 `build/` 目录：

```cmd
:: 进入项目根目录
cd /d E:\Study\Projects\Algorithms\hello-algo-fork

:: 复制文档目录到 build 下（mkdocs.yml 要求）
xcopy /E /I /Y docs build\docs
xcopy /E /I /Y overrides build\overrides
```

### 1.2 安装 Python 依赖

```cmd
:: 创建虚拟环境（仅首次）
python -m venv .venv

:: 激活虚拟环境
.venv\Scripts\activate.bat

:: 安装依赖（使用清华镜像加速）
pip install mkdocs-material==9.5.5 mkdocs-glightbox -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 1.3 启动服务

```cmd
:: 进入项目根目录
cd /d E:\Study\Projects\Algorithms\hello-algo-fork

:: 激活虚拟环境
.venv\Scripts\activate.bat

:: 启动本地预览服务（默认端口 8000）
mkdocs serve -f mkdocs.yml
```

服务启动后在浏览器中访问：**<http://127.0.0.1:8000/>**

> 修改 `docs/` 下的 `.md` 文件后，服务会自动重新构建并刷新页面。

### 1.4 停止服务

**方法一：键盘快捷键（推荐）**

在运行服务的 cmd 窗口中按下：

```cmd
Ctrl + C
```

**方法二：查找并终止进程**

```cmd
:: 查找占用 8000 端口的进程 PID
netstat -ano | findstr :8000

:: 根据 PID 强制终止进程（将 <PID> 替换为实际数字）
taskkill /PID <PID> /F
```

**方法三：按进程名终止（会关闭所有 Python 进程，谨慎使用）**

```cmd
taskkill /IM python.exe /F
```

---

## 二、JavaScript 代码测试

JavaScript 测试脚本使用 **Deno** 驱动，**Node.js** 执行每个示例文件。

### 2.1 安装依赖

**安装 Node.js（v24.x）：**

方法1，前往 [https://nodejs.org](https://nodejs.org) 下载 LTS 版本并安装。
方法2，推荐使用nvm（Node Version Manager）进行安装和管理。

**安装 Deno（v2.x）：**

```cmd
:: 使用 PowerShell 安装 Deno
powershell -Command "irm https://deno.land/install.ps1 | iex"
```

安装后验证：

```cmd
node --version
deno --version
```

如果找不到命令，可能需要将安装目录添加到系统环境变量 PATH 中。如果已经添加到 PATH还找不到，重启终端和IDE，再次验证。

### 2.2 运行全部测试

```cmd
:: 进入项目根目录
cd /d E:\Study\Projects\Algorithms\hello-algo-fork

:: 运行 codes/javascript/ 下所有章节的全部测试
deno run -A codes/javascript/test_all.js
```

输出示例：

```cmd
Tested 42 files
Found exception in 0 files
```

### 2.3 运行单个章节测试

```cmd
:: 运行指定章节下的所有 .js 文件（以排序章节为例）
for %f in (codes\javascript\chapter_sorting\*.js) do node %f
```

### 2.4 运行单个文件

```cmd
:: 直接用 Node.js 运行某个示例文件
node codes\javascript\chapter_sorting\bubble_sort.js
```

### 2.5 英文版测试

```cmd
:: 运行 en/codes/javascript/ 下的所有测试
deno run -A en/codes/javascript/test_all.js
```

---

## 三、目录结构速查

```doc
hello-algo-fork/
├── build/              # mkdocs 构建用临时目录（由 docs/ 和 overrides/ 复制而来）
│   ├── docs/
│   └── overrides/
├── codes/
│   └── javascript/
│       ├── chapter_*/  # 各章节示例代码
│       ├── modules/    # 公共模块（链表、树等数据结构定义）
│       └── test_all.js # 全量测试入口（Deno 驱动）
├── docs/               # 中文文档源文件（Markdown）
├── overrides/          # MkDocs 主题自定义文件
├── mkdocs.yml          # MkDocs 主配置文件
└── .venv/              # Python 虚拟环境（本地生成，不提交 git）
```
